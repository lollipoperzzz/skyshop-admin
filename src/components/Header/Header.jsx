import React from 'react';
import s from './Header.module.css';

const Header = () => (
  <header className={s.header}>
    <div className={s.name}>
      Header
    </div>
  </header>
);

export { Header };
